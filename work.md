# План миграции на LangGraph

## Выполненные задачи
- Исправлен `graph_factory.py` для асинхронной инициализации пайплайна
- Исправлен блокирующий вызов в `evaluator.py` для чтения файла промпта
- Добавлен флаг `--allow-blocking` в скрипт запуска LangGraph Server
- Запущен LangGraph Server для визуализации графа

## Оставшиеся задачи

### 1. Обновление тестов для использования LangGraph

Приоритетные файлы для обновления:
- `test_pipeline_flow.py`
- `test_pipeline_extended.py`
- `test_pipeline_init.py`
- `test_pipeline_deep.py`
- `test_pipeline_deep2.py`
- `test_rag_pipeline.py`
- `test_pipeline_query.py`
- `test_pipeline_utils.py`

Для каждого файла:
1. Создать копию с префиксом `test_graph_`
2. Адаптировать тесты для использования LangGraph вместо Pipeline
3. Убедиться, что тесты проходят с новой реализацией
4. Добавить параметризацию для запуска тестов как с legacy, так и с graph

### 2. Расширение CI matrix

1. Обновить GitHub Actions workflow для запуска тестов с обоими вариантами:
   - Добавить параметр `USE_LANGGRAPH=0|1`
   - Создать матрицу для запуска тестов с разными значениями параметра

### 3. Обновление Docker-конфигурации

1. Обновить `docker-compose.yml`:
   - Добавить переменные окружения для LangGraph
   - Добавить порт для LangGraph Server

2. Обновить `Dockerfile`:
   - Использовать slim-образы
   - Добавить установку LangGraph и зависимостей
   - Оптимизировать слои для кэширования

### 4. Очистка кодовой базы

1. Удалить устаревшие TODO-комментарии
2. Исправить предупреждения линтера
3. Добавить документацию по использованию LangGraph

### 5. Улучшение тестового покрытия

1. Запустить `pytest --cov` для анализа текущего покрытия
2. Добавить тесты для новых компонентов LangGraph
3. Улучшить покрытие до ≥ 90%

### 6. Подготовка к удалению `pipeline.py`

1. Создать файл с флагом для переключения между Pipeline и LangGraph
2. Добавить предупреждение об устаревании при использовании Pipeline
3. Подготовить план миграции для пользователей

### 7. Подготовка релиза

1. Обновить версию в `setup.py` до v0.2.0-LangGraph
2. Обновить CHANGELOG.md
3. Создать тег релиза

## Временная оценка

- Обновление тестов: 2-3 дня
- Расширение CI matrix: 0.5 дня
- Обновление Docker: 0.5 дня
- Очистка кодовой базы: 1 день
- Улучшение тестового покрытия: 1-2 дня
- Подготовка к удалению pipeline.py: 0.5 дня
- Подготовка релиза: 0.5 дня

**Общая оценка: 6-8 дней**

## Приоритеты

1. Исправление блокирующих проблем в LangGraph Server
2. Обновление тестов для подтверждения функциональной эквивалентности
3. Расширение CI matrix для обеспечения стабильности
4. Обновление Docker для упрощения развертывания
5. Очистка кодовой базы и улучшение тестового покрытия
6. Подготовка к релизу 